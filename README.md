# Data Structures & Algorithms – Lab Work in C++
This repository contains my complete Data Structures & Algorithms (DSA) lab work, practice programs, and past-paper solutions developed in C++ during my BSCS studies at the University of Karachi.

This repo holds all my lab work, practice code, and past-paper solutions for my Data Structures & Algorithms course at the University of Karachi, Department of Computer Science. All implementations in this repository are written in C++.

## About This Repo

This repository contains my Data Structures & Algorithms lab work, practice problems, and past-paper solutions completed during the semester. Each topic is organized into its own folder, with multiple implementations where applicable.

## Author Details

- **Name:** Areeba
- **Program / Semester:** BSCS 3rd Semester, Morning
- **Course Code:** CS-451
- **Instructor:** Miss Maryam Feroze
- **Session:** 2025 – 2029

## Folder Overview

| Folder | What's Inside |
|---|---|
| `AVLTree` | Self-balancing BST — insertion, rotations, balancing |
| `BFS` | Breadth-first search traversal |
| `BinarySearch` | Binary search implementation |
| `BinarySearchTree` | BST insertion, deletion, search, traversal |
| `BubbleSort` | Bubble sort |
| `DFS` | Depth-first search traversal |
| `EXPRESSION_TREE(FROM_POSTFIX)` | Building an expression tree from postfix notation |
| `ExpressionTree` | Expression tree construction and evaluation |
| `Graph` | Graph representations and traversal algorithms |
| `Hashing` | Hash tables — linear probing, quadratic probing, chaining |
| `Heap` | Max heap, min heap, heap sort |
| `InsertionSort` | Insertion sort |
| `LAB-PAST-PAPERS` | Solved past lab exam papers |
| `Level_Order_Traversal` | Level-order tree traversal |
| `LinearSearch` | Linear search |
| `LinkedList` | Singly, doubly, and circular linked lists |
| `LL_RECURSION` | Recursive linked list operations |
| `MergeSort` | Merge sort |
| `MultiLinkList` | Multi-linked list structures |
| `PostFix_Evaluation` | Postfix expression evaluation |
| `PRACTICE_FOR_PAPERS` | Extra practice questions for exam prep |
| `PrefixEvaluation` | Prefix expression evaluation |
| `Queue` | Array-based, linked-based, and circular queues |
| `QuickSort` | Quick sort |
| `SelectionSort` | Selection sort |
| `SORTSINLINKEDLIST` | Sorting algorithms applied to linked lists |
| `SpannigTree` | Spanning tree algorithms |
| `Stack` | Stack — array-based and linked-based implementations |

## Running the Code

Each `.cpp` file is standalone, so you don't need to link anything from other folders. Just go into the folder you want and compile:

```bash
g++ ProgramName.cpp -o output
./output
```

On Windows:

```bash
g++ ProgramName.cpp -o output.exe
output.exe
```

## Notes

- This repo is updated regularly as new labs and assignments come in.
- File names usually tell you exactly what's implemented (e.g. `circular_linked_list.cpp`, `max_heap.cpp`).
- This repository is intended primarily for learning, revision, and academic practice. If you use any of the code, please make sure you understand how it works rather than copying it directly for coursework.
